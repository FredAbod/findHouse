╔══════════════════════════════════════════════════════════════════════════════╗
║                   FINDHOUSE API - AZURE DEPLOYMENT                           ║
║                     Quick Reference for DevOps                               ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│ 📋 QUICK START (3 Commands)                                                 │
└──────────────────────────────────────────────────────────────────────────────┘

1. Login to Azure
   $ az login

2. Create Resources
   $ ./deploy.sh

3. Deploy (via GitHub Actions)
   $ git push origin main


┌──────────────────────────────────────────────────────────────────────────────┐
│ 💰 COST STRUCTURE (Student Optimized)                                       │
└──────────────────────────────────────────────────────────────────────────────┘

┌────────────────────────┬─────────────┬─────────────┬──────────────────────┐
│ Component              │ Free Tier   │ Production  │ Recommendation       │
├────────────────────────┼─────────────┼─────────────┼──────────────────────┤
│ App Service            │ F1 ($0)     │ B1 ($13/mo) │ Use F1 for dev       │
│ Database               │ Atlas M0    │ Atlas M0    │ Stay on free tier    │
│ CI/CD                  │ GitHub Free │ GitHub Free │ 100% free            │
│ Storage (Images)       │ Cloudinary  │ Cloudinary  │ 10GB free            │
│ Storage (Videos)       │ Drive Free  │ Drive Free  │ 15GB free            │
│ Monitoring             │ Insights    │ Insights    │ 5GB/month free       │
├────────────────────────┼─────────────┼─────────────┼──────────────────────┤
│ TOTAL                  │ $0/month    │ $13/month   │ 7 months on $100     │
└────────────────────────┴─────────────┴─────────────┴──────────────────────┘


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔧 ESSENTIAL COMMANDS                                                        │
└──────────────────────────────────────────────────────────────────────────────┘

Resource Group:
  Create:  az group create --name findhouse-rg --location eastus
  Delete:  az group delete --name findhouse-rg --yes
  List:    az group list --output table

App Service:
  Create:  az webapp create --name <app-name> --resource-group findhouse-rg \
           --plan findhouse-plan --runtime "NODE:18-lts"
  Start:   az webapp start --name <app-name> --resource-group findhouse-rg
  Stop:    az webapp stop --name <app-name> --resource-group findhouse-rg
  Restart: az webapp restart --name <app-name> --resource-group findhouse-rg
  Delete:  az webapp delete --name <app-name> --resource-group findhouse-rg

Logs:
  Stream:  az webapp log tail --name <app-name> --resource-group findhouse-rg
  Deploy:  az webapp log deployment show --name <app-name> --resource-group findhouse-rg

Configuration:
  List:    az webapp config appsettings list --name <app-name> --resource-group findhouse-rg
  Set:     az webapp config appsettings set --name <app-name> --resource-group findhouse-rg \
           --settings KEY=VALUE
  Delete:  az webapp config appsettings delete --name <app-name> --resource-group findhouse-rg \
           --setting-names KEY

Deployment:
  Manual:  ./scripts/quick-deploy.sh <app-name>
  Profile: az webapp deployment list-publishing-profiles --name <app-name> \
           --resource-group findhouse-rg --xml


┌──────────────────────────────────────────────────────────────────────────────┐
│ 📁 PROJECT STRUCTURE                                                         │
└──────────────────────────────────────────────────────────────────────────────┘

findHouse/
├── 📄 AZURE_DEPLOYMENT.md          ← START HERE (Complete guide)
├── 📄 README_DEPLOYMENT.md         ← DevOps summary
├── 📄 DEPLOYMENT_CHECKLIST.md      ← Step-by-step checklist
├── 📄 QUICKSTART.txt               ← This file
│
├── 🔧 azure-pipelines.yml          ← Azure DevOps pipeline
├── 🔧 .github/workflows/
│   └── azure-deploy.yml            ← GitHub Actions (RECOMMENDED)
│
├── 🐳 Dockerfile                   ← Container deployment (optional)
├── 🐳 .dockerignore
│
├── 📝 deploy.sh                    ← Automated resource creation
├── 📝 scripts/
│   ├── quick-deploy.sh             ← Manual deployment
│   └── monitor-costs.sh            ← Weekly cost monitoring
│
├── 🔐 .env.production              ← Production env template
└── src/                            ← Application code


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🚀 DEPLOYMENT WORKFLOWS                                                      │
└──────────────────────────────────────────────────────────────────────────────┘

Option 1: GitHub Actions (Recommended)
  ┌─────────────────────────────────────────────────────────────────┐
  │ 1. Create Azure resources:        ./deploy.sh                   │
  │ 2. Get publish profile:           az webapp deployment list-... │
  │ 3. Add to GitHub Secrets:         AZURE_WEBAPP_PUBLISH_PROFILE  │
  │ 4. Update workflow file:          .github/workflows/...         │
  │ 5. Push to main:                  git push origin main          │
  │ 6. Auto-deploys on every push                                   │
  └─────────────────────────────────────────────────────────────────┘
  ✅ Free, Simple, Perfect for students

Option 2: Azure DevOps
  ┌─────────────────────────────────────────────────────────────────┐
  │ 1. Create Azure DevOps project                                  │
  │ 2. Create service connection                                    │
  │ 3. Import azure-pipelines.yml                                   │
  │ 4. Configure environment                                        │
  │ 5. Run pipeline                                                 │
  └─────────────────────────────────────────────────────────────────┘
  ✅ Better Azure integration, More enterprise features

Option 3: Manual Deployment
  ┌─────────────────────────────────────────────────────────────────┐
  │ ./scripts/quick-deploy.sh <app-name>                            │
  └─────────────────────────────────────────────────────────────────┘
  ✅ No pipeline needed, Direct control


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🔐 REQUIRED ENVIRONMENT VARIABLES                                            │
└──────────────────────────────────────────────────────────────────────────────┘

Essential:
  ✓ NODE_ENV=production
  ✓ PORT=8080
  ✓ MONGODB_URI=mongodb+srv://...
  ✓ JWT_SECRET=<min-32-chars>

Services:
  ✓ CLOUDINARY_CLOUD_NAME
  ✓ CLOUDINARY_API_KEY
  ✓ CLOUDINARY_API_SECRET
  ✓ GOOGLE_DRIVE_CLIENT_EMAIL
  ✓ GOOGLE_DRIVE_PRIVATE_KEY
  ✓ EMAIL_HOST, EMAIL_PORT, EMAIL_USER, EMAIL_PASSWORD
  ✓ SUPPORT_EMAIL
  ✓ FRONTEND_URL

Set via:
  Portal:  App Service → Configuration → Application settings
  CLI:     az webapp config appsettings set ...


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🐛 TROUBLESHOOTING                                                           │
└──────────────────────────────────────────────────────────────────────────────┘

App won't start:
  $ az webapp log tail --name <app-name> --resource-group findhouse-rg
  → Check: PORT=8080, NODE_ENV=production, start script

Database connection failed:
  → Check: MongoDB Atlas IP whitelist (0.0.0.0/0)
  → Verify: Connection string in env vars
  → Test: Connection locally first

Deployment failed:
  $ az webapp log deployment show --name <app-name> --resource-group findhouse-rg
  → Check: Build logs in pipeline
  → Verify: All dependencies in package.json

High costs:
  $ ./scripts/monitor-costs.sh
  → Use: F1 tier for development
  → Stop: App when not in use (B1+)
  → Review: Cost Management dashboard

Can't access app:
  → Verify: https://<app-name>.azurewebsites.net
  → Check: Health endpoint: /health
  → Test: curl https://<app-name>.azurewebsites.net/health


┌──────────────────────────────────────────────────────────────────────────────┐
│ 📊 MONITORING                                                                │
└──────────────────────────────────────────────────────────────────────────────┘

Health Check:
  $ curl https://<app-name>.azurewebsites.net/health

Live Logs:
  $ az webapp log tail --name <app-name> --resource-group findhouse-rg

Weekly Cost Review:
  $ ./scripts/monitor-costs.sh

Application Insights:
  Portal → App Service → Application Insights → Live Metrics

Cost Management:
  https://portal.azure.com/#view/Microsoft_Azure_CostManagement


┌──────────────────────────────────────────────────────────────────────────────┐
│ 💡 COST-SAVING TIPS                                                          │
└──────────────────────────────────────────────────────────────────────────────┘

1. Development: Use F1 (Free) tier
   $ az appservice plan update --name findhouse-plan --resource-group findhouse-rg --sku F1

2. Stop app when not in use (B1+):
   $ az webapp stop --name <app-name> --resource-group findhouse-rg

3. Use MongoDB Atlas M0 (free):
   → Free forever, 512MB storage

4. Set budget alerts:
   Portal → Cost Management → Budgets → Add alert at $30/month

5. Delete unused resources:
   $ az group delete --name findhouse-rg --yes

6. Monitor weekly:
   $ ./scripts/monitor-costs.sh


┌──────────────────────────────────────────────────────────────────────────────┐
│ 📚 DOCUMENTATION FILES                                                       │
└──────────────────────────────────────────────────────────────────────────────┘

For Beginners:
  1. AZURE_DEPLOYMENT.md        ← Complete step-by-step guide
  2. DEPLOYMENT_CHECKLIST.md    ← Interactive checklist
  3. README_DEPLOYMENT.md       ← Overview and architecture

For DevOps:
  1. README_DEPLOYMENT.md       ← Architecture decisions
  2. azure-pipelines.yml        ← Pipeline configuration
  3. scripts/                   ← Automation scripts

Quick Reference:
  1. QUICKSTART.txt             ← This file


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🆘 SUPPORT                                                                   │
└──────────────────────────────────────────────────────────────────────────────┘

Azure Student Support:
  Email: azureforedu@microsoft.com
  Portal: https://portal.azure.com → Help + Support

Resources:
  Documentation: See AZURE_DEPLOYMENT.md
  Cost Issues: ./scripts/monitor-costs.sh
  Deployment: ./scripts/quick-deploy.sh <app-name>


┌──────────────────────────────────────────────────────────────────────────────┐
│ ✅ DEPLOYMENT CHECKLIST (Quick)                                              │
└──────────────────────────────────────────────────────────────────────────────┘

Pre-Deployment:
  [ ] Azure CLI installed (az --version)
  [ ] Logged in to Azure (az login)
  [ ] MongoDB Atlas cluster created
  [ ] External services configured (Cloudinary, Google Drive, Email)

Azure Setup:
  [ ] Run ./deploy.sh
  [ ] Note your app name
  [ ] Set environment variables

CI/CD Setup:
  [ ] Get publish profile (for GitHub Actions)
  [ ] Add GitHub secret
  [ ] Update workflow file
  [ ] Push to main branch

Post-Deployment:
  [ ] Test /health endpoint
  [ ] Verify API endpoints
  [ ] Check logs for errors
  [ ] Set up cost alerts
  [ ] Schedule weekly monitoring


┌──────────────────────────────────────────────────────────────────────────────┐
│ 🎯 RECOMMENDED PATH                                                          │
└──────────────────────────────────────────────────────────────────────────────┘

1. Read:    AZURE_DEPLOYMENT.md (30 minutes)
2. Prepare: MongoDB Atlas + External services (20 minutes)
3. Create:  Azure resources with ./deploy.sh (10 minutes)
4. Deploy:  Set up GitHub Actions (15 minutes)
5. Test:    Verify deployment (10 minutes)
6. Monitor: Run ./scripts/monitor-costs.sh weekly

Total Time: ~85 minutes
Total Cost: $0-13/month
Credit Duration: 7+ months


╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║  🚀 Ready to Deploy? Start with: ./deploy.sh                                ║
║                                                                              ║
║  💰 Estimated Cost: $0/month (F1 + MongoDB M0)                              ║
║                     $13/month (B1 + MongoDB M0) for production              ║
║                                                                              ║
║  📖 Full Guide: Open AZURE_DEPLOYMENT.md                                    ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝
